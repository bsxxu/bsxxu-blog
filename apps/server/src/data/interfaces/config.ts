export type DbConfig = {
  name: string;
};

export type ServerConfig = {
  port: number;
};

export type SearchConfig = {
  host: string;
  apikey: string;
};

export type CommonConfig = {
  postsPath: string;
};
